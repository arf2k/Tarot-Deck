import ShuffleActionTypes from "./shuffleTypes";

const INITIAL_STATE = {
     shuffle : []
}

const shuffleReducer = (state = INITIAL_STATE, action) => {
     switch(action.type) {
          case 
     }
}